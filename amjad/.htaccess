# mod_rewrite starts here

RewriteEngine on

RewriteCond %{REQUEST_FILENAME} !_d

# check the file in the directory with .html extension

RewriteCond %{REQUEST_FILENAME}\.html -f

# Here we actually show the page which has the .html extension

RewriteRule ^(.*)$ $1.html [NC,L]

